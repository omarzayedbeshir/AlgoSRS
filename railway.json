{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && go build -o lc-fsrs-backend ."
  },
  "deploy": {
    "startCommand": "./backend/lc-fsrs-backend",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 10
  }
}
